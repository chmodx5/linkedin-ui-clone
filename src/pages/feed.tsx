import React from "react";
import Button from "@mui/material/Button";
const Feed = () => {
    return (
        <div>
            Feed
            <Button variant="contained">do me</Button>
        </div>
    );
};

export default Feed;
